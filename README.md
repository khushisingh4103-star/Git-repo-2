# Functionality of programs 

In the 1st program, a variable "n" is created to store the user input.
And then a function "fac" is created in which there's a variable named "res" which will store the final result.
After this another variable "f" is created to store the value of the function "fac" and is printed as an output. 

In the 2nd program, initially the math class is imported as we have to use functions contained inside it.
After this a variable named "n" is created to store user input.
And then 3 more variables are created to store the value of square root, logarithm & sine.
Lastly, all of these 3 values are printed as an output.
